<% content_for :title, "All users" %>

<table class="users">
  <tr>
    <th>Count</th>
    <th>Realname</th>
    <th>Username</th>
    <th>Email</th>
    <th>Created at</th>
  </tr>
  <% i = 0 %>
  <% @users.each do |u| %>
  <% i += 1 %>
  <tr>
    <td><%= i %></td>
    <td><%= u.realname %></td>
    <td><%= u.username %></td>
    <td><%= u.email %></td>
    <td><%= u.created_at %></td>
  </tr>
  <% end %>
</table>
