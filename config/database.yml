# SQLite version 3.x
#   gem install sqlite3
development:
  adapter: sqlite3
  database: db/development.sqlite3
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: db/test.sqlite3
  pool: 5
  timeout: 5000

production:
  adapter: sqlite3
  database: db/production.sqlite3
  pool: 5
  timeout: 5000

# The following configurations are for the user database
# which is separated from this application and is supposed to
# be managed by another application targeting user management.
#
# For details, follow the following link:
# http://stackoverflow.com/questions/1226182/

user_development:
  adapter: mysql
  database: enterprise_wiki_users
  username: root
  password: root
  host: localhost

user_test:
  adapter: mysql
  database: enterprise_wiki_users
  username: root
  password: root
  host: localhost

user_production:
  adapter: mysql
  database: enterprise_wiki_users
  username: root
  password: root
  host: localhost

